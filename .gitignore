# Python
__pycache__/
*.pyc
.venv/

# Node
node_modules/

# Claude Code local state
.claude/settings.local.json
.claude/*.tmp.*

# Unscrubbed copy of the project brief
CLAUDE.local.md

# Exports from recording / save-data testing
scratch/
*.tmp
*.log

# Archive the repo was bootstrapped from
hito-seed.zip

# OS
.DS_Store
Thumbs.db

# Personal command-line study tool and its log, local only
vocab/study.py
vocab/attempts.jsonl

# Dropped in by `appwrite init project`: an agent skill for the Appwrite CLI
# and the symlink Claude Code reads it through. Useful locally, not ours to
# publish.
.agents/
.claude/skills/

# The agents' own venv, the pulled table, and what they write
agents/.venv/
agents/data/
agents/out/
agents/*.egg-info/

# Keys for the agents (Bedrock, OpenRouter). The agents read these files
# themselves; nothing else in the repo holds or needs a secret.
.env
agents/.env
